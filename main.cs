using System;

namespace PrimeraApp
{
    class Program
    {
        static void Main(String [] args)
        {
            Console.Writeline("Hola mundo");
            // git clone <clone>, clonar un repositorio en mi computadora para manejar el proyecto de manera local
            // git fetch, despues de clonar el repositorio, descarga todos los archivos
            // git pull origin main, movia al ulitmo commit
            // git commit -am "mensaje", guarda de manera local los cambios 
            // git push origin main, todos los commits los empuja en github 
        }
    }
}